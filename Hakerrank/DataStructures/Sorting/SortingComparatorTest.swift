//
//  SortingComparatorTest.swift
//  Hakerrank
//
//  Created on 2/27/20.
//  Copyright © 2020 Max. All rights reserved.
//

import Foundation

class SortingComparatorTest: PracticeTest {
    
    /*
     Python code:
     
     class Player:
        def __init__(self, name, score):
            self.name = name
            self.score = score
    
        def __repr__(self):
            return {'name': self.name, 'score': self.score}
     
        def comparator(a, b):
            if a.score != b.score:
                return b.score - a.score
            else:
                if b.name < a.name:
                    return 1
                else:
                    return -1
     
     */
}
